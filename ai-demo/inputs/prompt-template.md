# AI Generation Prompt Template

Use this existing framework.

Do not create a new framework.

Read `AGENTS.md` first.

Read the files in `ai-demo/context/` before editing source.

Generate framework-compatible automation only.

Use Cucumber feature files, thin step definitions, and page objects.

Reuse `BasePage.js` and existing utilities.

Do not add LLM API plumbing, fake AI commands, mock LLM plans, or `src/ai` runtime code.

Run tests using the scripts in `package.json`.

Fix high-confidence generated-code failures.

Create a run evidence folder under `ai-demo/runs/<timestamp>/` with the final report.
